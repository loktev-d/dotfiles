return {
    ensure_installed = { "lua", "go", "yaml", "json" },
    sync_install = true,
    auto_install = true,
    highlight = {
        enable = true,
        additional_vim_regex_highlighting = true,
    },
}
